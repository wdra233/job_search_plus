import React, {Component} from 'react';
import {connect} from 'react-redux';

class JobSeekerInfo extends Component {
    render() {
        return (
            <div>JobSeekerInfo</div>
        )
    }
}

export default connect(
    state => ({}),
    {}
)(JobSeekerInfo);